// Este archivo existe solo para que el tab de cámara aparezca en el navbar
// El contenido real está en app/camera.tsx (fuera de tabs)
// La navegación se maneja en _layout.tsx con listeners
export default function CameraTab() {
  return null;
}
